function LR_dist(eptrials, clusts, incl_trials, binsize, minvis)
%function [] = LR_dist()
%
%LR_dist finds the distance between the instantaneous population rep and
%the session representation of that pixle from each L and R trials and then
%outputs the difference between them
%
%


%which bins were visted at least MINVIS times on each trial type?
%output




end